<h2>You're almost done! Now create a password to securely access your account.
  <%= form_for(resource, as: resource_name, url: create_password_path, method: 'put') do |f| %>
    <%= devise_error_messages! %>
    <%= f.password_field :password, autofocus: true %>
    <%= f.password_field :password_confirmation %>
    <%= f.hidden_field :confirmation_token, value: @original_token %>
    <%= f.submit :submit, label: 'Confirm Account' %>
  <% end %>
</h2>
